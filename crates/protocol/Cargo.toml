[package]
name = "xenochat-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
xenochat-core = { path = "../core" }

[lints]
workspace = true
